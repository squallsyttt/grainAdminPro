# Claude Code 项目配置

## 🚨 关键规则：Codex MCP 优先使用策略（最高优先级）

**核心原则**：在所有需要深度思考、分析和决策的场景中，**必须优先使用 Codex MCP**，而不是 sequential thinking。

### 强制使用 Codex MCP 的场景

以下场景**必须**使用 `mcp__codex__codex` 工具：

1. **需求分析阶段**
   - 分析用户需求的优先级排序
   - 识别需求中的模糊点和冲突
   - 设计澄清问题和选项
   - 评估需求对系统的影响

2. **架构设计决策**
   - 技术栈选择和对比
   - 系统架构方案设计
   - 数据模型设计
   - API 设计和接口定义

3. **复杂问题分析**
   - 多方案对比和权衡
   - 性能优化策略制定
   - 安全方案设计
   - 集成方案评估

4. **任务规划**
   - 将需求分解为实现任务
   - 评估任务依赖关系
   - 估算工作量和风险

5. **代码审查和重构**
   - 分析现有代码质量
   - 提出重构建议
   - 评估技术债务

### ❌ 禁止使用 sequential thinking 的场景

**绝对禁止**在以下场景使用 `mcp__thinking__sequentialthinking`：
- 需要深度分析和决策的任务（必须用 Codex MCP）
- 需要考虑多个因素和权衡的问题（必须用 Codex MCP）
- 涉及架构、设计、规划的工作（必须用 Codex MCP）

### ✅ sequential thinking 的有限使用场景

**仅在**以下简单场景可以使用 sequential thinking：
- 简单的逻辑推理（如计算、格式转换）
- 单一明确的操作步骤（如文件读写顺序）
- 不涉及决策的纯执行性任务

### 执行要求

- **在开始任何分析或决策任务前**，必须先评估是否需要使用 Codex MCP
- **如有疑问**，优先选择 Codex MCP
- **每次会话**都要遵守此规则，不得因上下文切换而忽略
- **用户反馈**此规则未执行时，立即停止当前方法并切换到 Codex MCP

### 示例对比

❌ **错误做法**：
```
使用 sequential thinking 分析 10 个需求的优先级
→ 结果：分析浅显，缺乏深度考虑
```

✅ **正确做法**：
```
使用 Codex MCP 分析 10 个需求的优先级
→ 调用 mcp__codex__codex 进行深度分析
→ 考虑架构影响、技术栈影响、业务影响等多维度
→ 返回结构化的优先级排序和理由
```

---

## 语言策略

**核心原则**：本项目所有交互和文档内容必须使用中文，禁止使用英文。

### 交互语言要求

- **对话语言**：与用户的所有对话必须使用中文
- **响应内容**：所有回复、说明、解释必须使用中文
- **错误提示**：错误信息和警告提示必须使用中文
- **文档生成**：生成的所有文档（.md, .txt 等）必须使用中文

### 代码相关语言规范

- **代码注释**：所有代码注释必须使用中文
- **函数/变量命名**：使用英文命名（编程规范），但配套注释必须用中文
- **文档字符串**：Python docstring、JSDoc 等必须使用中文
- **提交信息**：Git commit message 使用中文

### 文档内容规范

生成以下类型文档时必须使用中文：
- README.md、SETUP.md 等项目文档
- spec.md、plan.md、tasks.md 等需求文档
- research.md、design.md 等技术文档
- API 文档、用户手册等
- 配置文件注释（.env.example 等）

### 特殊场景处理

- **技术术语**：可保留英文术语但需中文说明，如 "FastAPI（Python 异步 Web 框架）"
- **专有名词**：产品名、技术栈名称保持原文，如 PostgreSQL、Redis
- **代码示例**：代码本身使用标准英文命名，但周围说明必须用中文

### 执行要求

- ✅ **必须遵守**：所有对话、文档、注释使用中文
- ❌ **禁止**：纯英文回复、英文文档、英文注释
- ⚠️ **例外**：代码变量名、函数名、SQL 关键字等编程语言本身要求的英文

## 思考模式设置

**默认深度思考要求**：对于以下类型的问题自动启用 ultrathink 模式：

- 技术架构设计和选型决策
- 复杂业务逻辑分析和建模
- 系统集成方案评估
- 性能优化策略制定
- 安全方案设计
- 多方案对比和权衡分析
- 涉及多个系统模块的复杂问题

## 自动判断规则

当用户问题包含以下关键词或场景时，主动使用 ultrathink 模式：

- "设计"、"架构"、"选择"、"比较"、"优化"
- 涉及多个技术栈的决策
- 需要权衡利弊的技术方案
- 复杂的业务流程设计

## 执行指令

在回答用户问题前，先判断是否符合上述条件：

- 如果符合：自动启用 ultrathink 模式进行深度分析
- 如果不确定：询问用户是否需要深度思考模式
- 简单的查询或确认性问题：直接回答

## Spec Kit 集成

在执行 `/constitution`、`/specify`、`/plan`、`/tasks`、`/analyze`、`/implement` 等命令时，默认启用 ultrathink 模式确保：

- 全面的需求分析
- 充分的技术方案考量
- 系统性的实现规划

## 工具协同策略

注意claude一定每次都要启用 ultrathink

- 复杂分析阶段：优先使用 Codex MCP 进行深度分析
- 代码实现阶段：Claude Code 快速编码实现
- 架构决策：Codex 分析 → Claude Code 验证可行性
- 简单修改：Claude Code 直接处理

### 网页内容获取工具选择

**问题**：系统中存在两个 fetch 工具，需要明确优先级

**工具清单**：
1. **mcp__fetch__imageFetch**（MCP fetch 服务器）- 优先使用
   - 功能强大，支持图片提取
   - 网站访问限制更少
   - 支持跨域资源
   - 可保存文件到本地（`~/Downloads/mcp-fetch/`）

2. **WebFetch**（Claude Code 内置）- 备用方案
   - 功能受限，部分网站无法访问（如 docs.claude.com）
   - 仅在 MCP fetch 不可用时使用

**使用策略**：
- 默认使用 `mcp__fetch__imageFetch`
- 参数 `images: false` - 仅获取文本内容
- 参数 `images: true` - 获取文本 + 图片
- 只有在 MCP fetch 明确不可用时才回退到 WebFetch

**验证**：MCP fetch 配置正常，历史成功案例存在

## 项目特定设置

**项目类型**：B2B2C 电商平台
**关键决策领域**：

- 多租户数据隔离策略
- API 设计和服务架构
- 前后端分离实现方案
- 支付和订单流程设计
- 用户权限和认证系统

当涉及以上领域的问题时，必须使用 ultrathink 模式进行深度分析。
