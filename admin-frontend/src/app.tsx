// 运行时配置文件
// Umi 4.x 的 app.tsx 只能导出有效的插件 key
// 有效的 keys: patchRoutes, patchClientRoutes, modifyContextOpts,
// modifyClientRenderOpts, rootContainer, innerProvider, i18nProvider,
// accessProvider, dataflowProvider, outerProvider, render, onRouteChange

// 暂时保持空文件，避免导出无效的 key
export {};